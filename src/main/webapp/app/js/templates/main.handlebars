<div class="row">
    <div class="col-md-12">
        <div class="mx-auto text-center mb-3 mt-3">
            <h3 class="pt-4">{{i18n "movies"}}</h3>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <div class="mx-auto">
            <div class="card">
                <div class="card-body px-0 py-0">
                    <div class="input-group mb-0">
                        <span class="input-group-prepend">
                            <button class="btn btn-outline-primary ux-filter-action border-light" type="button">
                                <span class="fa fa-search"></span>
                            </button>
                        </span>
                        <input type="text" class="form-control ux-filter-field border-light" placeholder="Search for anything">
                        <div class="input-group-append">
                            <button class="btn btn-outline-primary dropdown-toggle border-light" type="button" id="dropdownMenuButton" data-toggle="dropdown">
                                <span class="ux-selected-filter">{{i18n "title"}}</span>
                            </button>
                            <div class="dropdown-menu" role="menu" aria-labelledby="dropdownMenuButton">
                                <a data-option="title" class="dropdown-item ux-filter">{{i18n "title"}}</a>
                                <a data-option="director" class="dropdown-item ux-filter">{{i18n "director"}}</a>
                                <a data-option="genre" class="dropdown-item ux-filter">{{i18n "genre"}}</a>
                                <a data-option="rating" class="dropdown-item ux-filter">{{i18n "rating"}}</a>
                                <a data-option="year" class="dropdown-item ux-filter">{{i18n "year"}}</a>
                            </div>
                        </div>
                    </div>
                    <table class="table">
                        <thead class="thead-gray">
                            <tr>
                                <th scope="col" class="font-weight-normal pl-4 pt-1 pb-1">{{i18n "title"}}</th>
                                <th scope="col" class="font-weight-normal pt-1 pb-1">{{i18n "director"}}</th>
                                <th scope="col" class="font-weight-normal pt-1 pb-1">{{i18n "genre"}}</th>
                                <th scope="col" class="font-weight-normal pt-1 pb-1">{{i18n "rating"}}</th>
                                <th scope="col" class="font-weight-normal pt-1 pb-1">{{i18n "year"}}</th>
                                <th scope="col" class="font-weight-normal pr-4"></th>
                            </tr>
                        </thead>
                        <tbody class="tbody-gray"></tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="ux-additional mx-auto row mb-3 mt-3">
            <button type="button" class="col-3 btn btn-primary ux-add-btn float-right">{{i18n "add.movie"}}</button>
        </div>
    </div>
</div>
